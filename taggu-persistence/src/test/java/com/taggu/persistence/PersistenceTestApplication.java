package com.taggu.persistence;

import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * A context root for the persistence tests.
 *
 * <p>The module has no application of its own, and Spring's test slices need somewhere to start from.
 */
@SpringBootApplication
class PersistenceTestApplication {}
